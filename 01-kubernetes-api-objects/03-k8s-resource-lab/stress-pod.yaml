apiVersion: v1
kind: Pod

metadata:
  name: stress-pod

spec:
  containers:
  - name: stress-container
    image: polinux/stress

    command:
    - stress

    args:
    - "--cpu"
    - "2"
    - "--vm"
    - "1"
    - "--vm-bytes"
    - "300M"
    - "--timeout"
    - "120s"