apiVersion: v1
kind: Pod

metadata:
  name: restart-demo

spec:
  restartPolicy: Always

  containers:
  - name: busybox
    image: busybox

    command:
    - sh
    - -c
    - "echo crash simulation && sleep 5 && exit 1"