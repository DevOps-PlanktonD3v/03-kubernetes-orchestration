apiVersion: v1
kind: Pod

metadata:
  # Nama client Pod
  name: busybox-client

spec:

  containers:
  - name: busybox

    image: busybox:1.36

    command:

    # Keep alive
    - sleep
    - "3600"